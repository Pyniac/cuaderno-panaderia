// Validaciones — se completan en el paso 8
const Validaciones = {
  // Por ahora vacío. Se llenará con:
  // - idUnico(recetas, id)
  // - idValido(id)
  // - categoriaExiste(categorias, cat)
  // - harinaTotalCoincide(receta)
  // - etc.
};
